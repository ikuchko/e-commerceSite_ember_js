import { cartItems } from 'e-commerce/helpers/cart-items';
import { module, test } from 'qunit';

module('Unit | Helper | cart items');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cartItems([42]);
  assert.ok(result);
});
